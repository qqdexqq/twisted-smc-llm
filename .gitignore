__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
build/
dist/

# Stage 0 run outputs (regenerable — see scripts/run_stage0_experiment.py)
results/
*.npz
figures/

# Stage 1+ rollout corpus: large, expensive to regenerate, but belongs on
# a persistent volume / object store, not in git (plan doc §11: "Use a
# persistent network volume for the HF cache and the rollout corpus").
data/
scratch_debug_mv.py
