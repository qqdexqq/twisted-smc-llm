name: adaptive-tsmc
channels:
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      - numpy
      - scipy
      - matplotlib
      - pytest
      - pyyaml
      - datasets          # Stage 1: pulling benchmark manifests
      - huggingface_hub
